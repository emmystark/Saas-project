1. To run the playground.ts, which is used to add/confirm the proper linkage of the postgress db, use --> "bun run ./src/playground.ts"
 
2.  "bun prisma db push".  to create a table on db using prisma 

3. "bun prisma studio"   to view the table created by prisma using a url 